# PROJECT-STATE — KB J Capital Intranet Rebuild (Second Brain)

> Any new session resumes from THIS FILE, never from scratch.
> Updated + committed at every stage transition by the Lead.

**Mission:** Build the new KBJ Capital intranet web portal to replace the
outdated legacy web. Doc-first; every gate/merge passes CTO verdict.

**Started:** 2026-09-10 · **Lead:** team-lead (GLM 5.3 session) · **CTO:** codex CLI (read-only gate)

---

## 1. Team roster & model routing (binding)

| Tier | Role | Runtime | Notes |
|---|---|---|---|
| Top | CTO | codex CLI 0.153.4, `--sandbox read-only` | FINAL verdict APPROVE/REVISE/REJECT on every gate/merge; auth/security/data lanes never merge without codex PASS |
| Middle | Lead / PM / Senior Dev | this session (glm-5.3) | Decomposition, task board, routing, triage, review + merge; SOLE channel for user directives |
| Worker ×5 | Dev positions | executor agents, sonnet tier (flash-class: cheap/fast/parallel) | Implement exactly per docs. Never edit docs, never merge, never commit to develop |

**Model routing:** deterministic checks first (tsc/tests/git — free) → worker
tier (80% of tokens) → lead tier (20%) → codex ONLY at milestone gates and
hard blocks.

**Escalation ladder:** worker fails >3 retries → middle tier retries ≤3 →
CTO (codex) solves directly.

## 2. Deliverable checklist (21 items — CTO must approve plan BEFORE vibe code)

| # | Deliverable | File / artifact | Status |
|---|---|---|---|
| 1 | Project Plan | `docs/deliverables/01-project-plan.md` | Approved (W1 gate 2026-09-10) |
| 2 | Risk Register | `docs/deliverables/02-risk-register.md` | Approved (W1 gate 2026-09-10) |
| 3 | SRS | `docs/deliverables/03-srs.md` | Approved (W1 gate 2026-09-10) |
| 4 | Requirement Traceability Matrix | `docs/deliverables/04-rtm.md` | Approved (W1 gate 2026-09-10) |
| 5 | SDS | `docs/deliverables/05-sds.md` | Approved (W1 gate 2026-09-10) |
| 6 | Architecture Diagram | `docs/deliverables/06-architecture-diagram.md` | Approved (W1 gate 2026-09-10) |
| 7 | Data Dictionary | `docs/deliverables/07-data-dictionary.md` | Approved (W1 gate 2026-09-10) |
| 8 | API Specification | `docs/deliverables/08-api-specification.md` | Approved (W1 gate 2026-09-10) |
| 9 | RBAC Design | `docs/deliverables/09-rbac-design.md` | Approved (W1 gate 2026-09-10) |
| 10 | Audit Log Design | `docs/deliverables/10-audit-log-design.md` | Approved (W1 gate 2026-09-10) |
| 11 | UI Prototype | `docs/deliverables/11-ui-prototype.md` | Approved (W1 gate 2026-09-10) |
| 12 | Test Plan | `docs/deliverables/12-test-plan.md` | Approved (W1 gate 2026-09-10) |
| 13 | Developed System | working system on `develop` | Wave 2 P0 chain merged 2026-09-11 (`c99abfb`, codex W2-GATE-2 PASS); W2-6 sync webhook + polish remain |
| 14 | Source Code | repo (`develop`) | ongoing |
| 15 | Database Script | `scripts/schema.sql` + migrations | exists / extend |
| 16 | Deployment Package | Dockerfile, compose, `k8s/` | exists / extend |
| 17 | System Test Result | `docs/deliverables/17-system-test-result.md` | Wave 3 |
| 18 | UAT Result | `docs/deliverables/18-uat-result.md` | Wave 3 |
| 19 | VA/Pentest Report | `docs/deliverables/19-va-pentest-report.md` | Wave 3 |
| 20 | Security Remediation Report | `docs/deliverables/20-security-remediation.md` | Wave 3 |
| 21 | User Manual | `docs/deliverables/21-user-manual.md` | Wave 3 |

## 3. Wave board (task → owner → status)

| Wave | Task | Owner | Status |
|---|---|---|---|
| 0 | Governance: branches, PROJECT-STATE, board | lead | done 2026-09-10 |
| 1 | Docs 01+02+11 (PM/UI lane) | worker-1 | done 2026-09-10 (+follow-up RISK-021) |
| 1 | Docs 03+04 (requirements lane) | worker-2 | done 2026-09-10 |
| 1 | Docs 05+06 (architecture lane) | worker-3 | done 2026-09-10 |
| 1 | Docs 07+08 (data/API lane) | worker-4 | done 2026-09-10 |
| 1 | Docs 09+10+12 (security/QA lane) | worker-5 | done 2026-09-10 |
| 1 | Lead review of doc set (consistency, cross-refs, no divergence) | lead | done 2026-09-10 |
| 1 | CTO gate: deliverable docs (substitute opus critic, per decision #4) | CTO | APPROVED 2026-09-10 (decision #6) — merged to develop |
| 2 | Implementation waves (decomposed from approved docs) | workers | unblocked — see .omc/handoffs/wave2-plan-draft.md |
| 2 | W2-1 dual-control bundle (DCR-3+7+AUD-P01/02/03) | worker-2 | CODE DONE 2026-09-10 — 22023eb on feature/w2-1-dual-control (workflow-field strip, state+identity guards, forced non-draft→draft reset, AUD-P01/02/03 audits; smoke 94/94). Holding with branch for codex |
| 2 | W2-2 audit-endpoint removal (DCR-8) | worker-5 doc / worker-2 code | COMPLETE 2026-09-10 — doc phase eb4c9b5 (develop) + code phase f6fa52d (branch): route + openapi post entry removed (404 catch-all for every role incl. admin; recordAudit sole audit writer), smoke 95/95, doc 09 v1.2.0. Merges with P0 chain at codex gate |
| 2 | W2-3 audit coverage gaps AUD-P06/05/07 | worker-5 | rulings issued (decision #7: AUD-P07 trimmed to presented-cookie 401s; audit-union prune CREATE/DELETE/SYNC_PUBLIC with UPDATE staying — single types.ts edit; CONTENT_* migration plan). DOC PHASE COMMITTED f569be1 (doc 10 v1.4.0 + 03-srs/04-rtm v1.3.0 FR-AUDIT-003 ext + doc 12 v1.6.0). Code phase COMPLETE — see code-phase row |
| 2 | W2-4 TypeScript strict (DCR-6) | worker-3 | Phases A+B DONE 2026-09-10 — root cause: @types/react{,-dom} missing (React=any, checker blind to frontend); with types: exactly 1 strict error (AdminCMS L331 union), fixed type-only. Lead landed feature/w2-4-strict-mode (3077e52 types+fix, 97bef96 strict flip; `tsc --noEmit` exit 0 incl. W2-1 in-flight tree). MERGED to develop 2026-09-10 (a07885e) — strict tsc is now the STANDING merge gate; independently verified by worker-3 in an isolated worktree (PASS matrix + isolation spot-check). **DCR-6 CLOSED** |
| 2 | W2-5 shared rate-limit store (RISK-010) | worker-1 | COMPLETE 2026-09-10 — code 1b237cd (repo +4 login-budget methods, PG atomic upsert, rate_limit_hits DDL lockstep, fail-open + degradation counter, smoke §15 opt-in PG mode; README §8 + HANDOVER §10 + doc 02 v1.3.0 RISK-010→Mitigating). Lead independent evidence: tsc --strict 0 · build 0 · smoke 95/95 · PG-mode 99/99. Doc flips (03 v1.4.0 FR-AUTH-002(d)(e) + 04 cell + 12 ENV row) landed 36ad1eb; security lane holds with branch for codex |
| 2 | W2-6 sync webhook (FR-SYNC-004) design + k8s egress | worker-4 | **DESIGN PHASE COMPLETE 2026-09-11 — v0.4** (.omc/research/w2-6-sync-webhook-design.md, 173/300 lines, sandbox-only): trigger map T1→T2→T4→T5; correlationId/eventId traceability (D6); HMAC-SHA256/TLS/5-min-skew; retry×2 + FAILED ledger; void-dispatch (D5, lead-amended); honest-UI rules; k8s egress template. **T5 withdrawal propagation** (codex advisory): scoped to the AUD-P01 synced→draft TRANSITION (canonical withdraw endpoint + PUT forced-reset secondary origin — lead ACCEPTED); identity-minimal payload {id,title}; dedupeKey keyed on transition timestamp (content-hash could dedupe a legitimate withdrawal away); FORCE_SYNC upgraded to DESIRED-STATE SNAPSHOT (receiver removes delivered-but-absent items — without it a lost T5 has NO replay path; lead ACCEPTED, now a public-web contract ask). DCR-10 resolved (§8). Code phase external-blocked: codex PASS + W2-1 merge + public-web blockers 1-4 (+ set-reconciliation in blocker 4); T5 additionally depends on W2-FIX-1 |
| 2 | W2-UX inert sync toggles (DCR-9) — doc-first pass | worker-3 | DONE 2026-09-10 — 36ad1eb (task #17, three lanes: doc 11 v1.1.0 target state G-1 closed; 05/06 v1.1.0 single-path truth pass; W2-5 flips 03 v1.4.0/04/12; + lead review fix doc 05 §8 row 3). Inventories 94/94/170 verified. Negative finding: 03 has no toggle text. externalCategory zero-consumer evidence → DCR-10 |
| 2 | DCR-9 code phase (Option C UI) | worker-4 | COMPLETE 2026-09-10 — **2d41c39** (task #19): toggles removed, "Withdraw from public" row action + confirm dialog, response-discard/fabricated-log/toast fixes + lead in-commit review fix (honest form headings + draft-until-approved subtext; 4 e2e selectors in lockstep). Lead gates: tsc --strict 0 · build 0 · smoke 99/99 · e2e 63/0/1 vs fresh prod server :3220 (dead-port e2e run diagnosed: suite requires QA-spawned server). Manifest sync **c7db581** (chore w2-4). **Worker-1 independent verification PASS** (all 4 checks; fresh gates re-run incl. e2e 63/0/1/0 exit 0; symbol sweep 0 hits incl. comments; wasNonDraft-before-await + AUD-P01 prior-status semantics confirmed vs server.ts; 2 non-blocking observations: defensive-but-honest unreachable toast branches; doc 11 anchors are §5.7+§5.8+G-1). Doc 11 v1.2.0 as-built flip landed (30d8476); gate-brief updated+verified by worker-2 (20-commit snapshot at 146111a; head drift to 6a6786d absorbed by the pre-gate checklist item); merge conflict map EMPIRICALLY confirmed by dry-run 2026-09-10 (conflicts: PROJECT-STATE.md + AdminCMS.tsx only; manifests/package files merge clean). Codex gate SCHEDULED 2026-09-15 09:05 (durable task 431b173f: pre-gate checklist → codex verdict → on APPROVE merge with mapped resolutions) |
| 2 | Docs 07/08 W2-1 as-built truth pass | worker-4 | COMPLETE 2026-09-10 — 1bc0d6b (task #18): doc 07 v1.2.0 + doc 08 v1.3.0 aligned to landed strict dual-control; stale-bypass divergence closed. W2-6 remains parked on external blocker |
| 2 | W2-3 audit coverage — CODE PHASE | worker-5 | COMPLETE 2026-09-10 — 4650335 on feature/w2-1-dual-control (5 files, +208/−9): AUD-P05 sync/trigger + AUD-P06 system/export (shared exportTimestamp) + AUD-P07 requireAuth presented-cookie 401 `if (token)` guard & requireRole async all-403 audit; types.ts:119 union prune (net 14 live writers); initialData audit-4 seed → SYNC_TRIGGER; AdminCMS dead SYNC_PUBLIC branches deleted; smoke §16 flip-pins (99/99; §16 runs before §14 — rate-limit key saturation). LEAD REVIEW FIX in-commit: requireRole 403 audit try/catch-wrapped (Express 4 rejects unwrapped middleware promises → 403 would hang on audit-store failure; fail-open consistent w/ W2-5). Lead evidence: tsc --strict 0 (prune proof) · build 0 · smoke 99/99. Worker-1 independent verification PASS (recorded 88ada8b; its one finding — stale smoke §16 docstring — fixed comment-only b5154b0). As-built doc flips LANDED 0be867c (doc 10 v1.5.0 + 03 v1.5.0 + 04 v1.5.0 [91 full + 1 partial] + 12 v1.8.0 [170 TC]; lead review ACCEPTED worker-5's two disclosed judgment calls). Ripple debt queued: doc 05 §3.5 action listing + doc 07 audit action list re-check at each doc's next revision (flagged doc 10 §9.2). Security lane holds with branch for codex |
| 2 | W2-FIX-1 codex blockers 1+2+4 + state-only withdraw endpoint (server+client+tests) | worker-2 | CODE DONE 2026-09-11 — **2c97cc3** (5 files +1025/−242): withNewsLock per-item mutex (all 6 news mutations, re-read-inside-lock, next(err) bridging); legacy-submission deny 409 (approve+reject, ACCESS_DENIED audit, existing union member); commitNewsTransition atomic state+audit (PG BEGIN/COMMIT+ROLLBACK / memory restore-on-failure, shared SQL consts); NEW POST /api/news/:id/withdraw (maker+, body ignored, synced-precondition 409, content byte-preserved, AUD-P01 atomic) + openapi; client state-only withdrawNews + 409 refresh-to-server-truth. Test hooks SMOKE_INJECT_AUDIT_FAILURE / SMOKE_SEED_W2FIX1_FIXTURES (non-prod-guarded, boot-time fixtures). Smoke §17: 9 checks, 4 spawned servers 3212-3215 (legacy deny, withdraw 409/happy+fingerprint, rollback proofs mem+PG, concurrency race mem+PG — single-server; the cross-pod gap is what W2-FIX-3 closes). LEAD REVIEW: all 6 flagged deviations ACCEPTED (fixture-seed hook justified — no API path manufactures the legacy shape; two-server PG split — inject poisons every commit). Lead gates: tsc --strict 0 · build OK · smoke 108/108 (10.9s) · secrets CLEAN. Worker evidence: PG opt-in 114/114 · e2e 63/0/1 exit 0. **Worker-1 independent verification PASS** (2026-09-11): all 4 blockers confirmed closed IN CODE (lock trace incl. deadlock check — no nested acquisition; guard order verified; withdraw body-never-referenced + fingerprint compare; byte-identical SQL consts verified; hooks double-guarded, seeded pre-listen). Fresh gates: tsc 0 · build 0 · smoke 108/108 (8.0s) · e2e 63/0/1 exit 0 (:3220 released) · PG opt-in 114/114 (14.4s, bonus). Race test hit BOTH interleavings across modes (memory approve=400, PG approve=200) — both ended draft+edited-content + AUD-P01. 3 non-blocking findings for the gate brief: (1) withNewsLock is per-process (documented server.ts:1398-99) — with 2 replicas under READ COMMITTED a cross-pod stale-overwrite window remains (no version column / FOR-UPDATE re-validation); §17 races hit ONE server; W2-5 note says 1 process/pod today. MUST ride W2-GATE-2 explicitly; candidate follow-up: optimistic version column or SELECT..FOR UPDATE re-read inside commitNewsTransition; (2) commit message says '5 spawned servers', actual 4 (3212-3215) — cosmetic, corrected here; (3) tree carried worker-5's in-flight 08 edit — expected, W2-FIX-2 lane |
| 2 | W2-FIX-2 doc bundle: 08 withdraw endpoint · 10 audit atomicity · 12 TCs+inventory · 03/04 rows · 05 §3.5 (closes ripple debt) | worker-5 | COMPLETE 2026-09-11 — **683a1ce** (6 docs +251/−88): 08 v1.4.0 (§6.8 withdraw + §6 concurrency/atomicity + legacy 409s + 39-op renumber + DCR-8 true-ups) · 10 v1.6.0 (§9.3 atomicity + two-write-paths) · 12 v1.9.0 (7 TCs TC-NEWS-013..019, 016+019 P0; inventory 177; catalog true-up) · 03 v1.6.0 (FR-NEWS-009 extended + NEW FR-NEWS-010; 94→95 REQ) · 04 v1.6.0 (Total 95 / 92+1 partial; DCR-9 annotations flipped) · 05 v1.2.0 (§3.5 14-value union + two write paths — **0be867c ripple debt CLOSED**). LEAD REVIEW ACCEPTED: Thai strings byte-exact, versions/changelogs verified, FR cross-links verified, all 3 judgment calls upheld (new FR id justified; P0 core proofs; catalog catch). Self-corrected DCR-9 attribution (UI=2d41c39 vs mechanics=2c97cc3) verified. Flip discipline decision #6 cond 2 honored — docs flipped in the same chain as the verified code. **Pre-gate cleanup 225a96a** (worker-2 brief flags 1+2): doc 07 v1.3.0 audit-action ripple pass (§3.2 14-value union byte-exact vs types.ts:119, lead-verified values+order; prune/DCR-8 provenance; W2-FIX-1 call-site shapes; §5.3 news workflow rows; §5.9 two write paths) + doc 12 v1.9.1 errata (spawned-server count 4, not 5) — **0be867c ripple debt FULLY closed (both halves)**. Branch head 225a96a, 31 commits develop..branch |
| 2 | W2-GATE-2 codex re-gate after fix verification | CTO | DONE 2026-09-11 — **REVISE (fix cycle 2)** at head `dda8268` (decision #9): blockers 2+4 CLOSED, 1+3 NOT CLOSED cross-pod (codex isolated two-pod repro; §7.6 ruled BLOCK — replicas:2 cannot ride); ALL §3 riders ratified (D7 overstatement correction noted); merge NOT authorized. Verdict: `.omc/artifacts/cto-gate-wave2-verdict-2.md` |
| 2 | W2-FIX-3 cross-pod concurrency (codex cycle-2 mandate): runNewsTransition tx executor | worker-2 (respawn) | CODE DONE 2026-09-11 — **bccd441** (task #23, server.ts + smoke-test.mjs, +882/−328): `runNewsTransition` single-tx executor (`SELECT … FOR UPDATE` re-read → sync/pure plan → state/audit/syncLog writes in ONE PG tx; memory mirror w/ restore-on-throw) replaces `commitNewsTransition`; all 5 writers + draft PUTs restructured (denial audits AFTER transition — recordAudit can't share the row lock); withNewsLock retained (same-pod); create/DELETE unchanged. Smoke §18 (opt-in PG, pods :3216/:3217 sharing ONE PostgreSQL = replicas:2 topology): both codex-reproduced races × (3 concurrent invariant iterations + 2 deterministic staggered exact-outcome iterations — all 4 legal branches pinned). Injection hook guarded `p.audit &&` (W2-FIX-1 draft-PUT semantics restored). LEAD REVIEW: accepted (D1 helper-factory, D2-D5 behavior-preserving; delta follow-ups verified at :355/:1070). **Independent verifier PASS** (worker-1 lane): tsc 0 · build 0 · smoke 108/108 · PG 117/117 · e2e 63/0/1 exit 0 (:3220 released) · secrets clean · zero request-path writers outside the executor · §17 contract byte-identical; findings F1 (§18 scheduler-dependent branch coverage) + F2 (hook fired for audit-less PUTs) both FIXED in bccd441. Observed: race1 concurrent approve=[400,200,200], race2 withdraw=[409,200,409], all 4 staggered branches exact. Standing gates GREEN on bccd441 (see docs-flip row). Pipeline: gate-brief §8 refresh → codex re-gate 3 (NO merge until PASS) |
| 2 | W2-FIX-3 docs flip (task #25) — 7 deliverable docs as-built + standing gates | worker-2 + lead fix | COMPLETE 2026-09-11 — **41894bc** (+130/−88): 03 v1.7.0 (FR-NEWS-009/010 trued) · 04 v1.7.0 (rows re-pinned, totals unchanged 95/92+1/UAT 62) · 05 v1.3.0 (§3.5 unified executor) · 07 v1.4.0 (§5.9) · 08 v1.5.0 (§6 concurrency rewritten) · 10 v1.7.0 (§9.3 blockers, hook truth) · 12 v1.10.0 (TC-NEWS-020/021/022, inventory 180). LEAD REVIEW: 3 worker judgment calls ACCEPTED (changelog annotate-not-rewrite; TC-NEWS-020 P1; doc 09 untouched); partial §2 anchor refresh OVERRIDDEN — lead re-anchored ALL remaining §2.2/§2.3 rows in-commit (bccd441 grew server.ts ~554 lines), split the auth/limiter row, module map MONOTONIC, every bound spot-checked vs server.ts. Standing gates on bccd441 (.omc/reports/w2fix3-standing-gates.log): tsc --strict 0 · build OK · smoke 108/108 (7.8s) · e2e 63/0/1 exit 0 (:3220 released) · PG 117/117 (16.9s; first attempt 108/112 was environmental — kbj_smoke DB not pre-created in fresh container, re-run green, container removed) · secrets develop..HEAD 0. codex verdict-3 (decision #10): REVISE fix cycle 3 — code CLOSED, §18 determinism NOT. NEXT: W2-FIX-4 (task #26) |
| 2 | W2-FIX-4 codex cycle-3 mandate: §18 deterministic restructure + negative control | worker-2 + lead fix | COMPLETE 2026-09-11 — **fb3d748** (task #26, 3 files +442/−330): server.ts two new PG-only non-prod hooks (SMOKE_DELAY_NEWS_COMMIT_MS — committed runNewsTransition sleeps N ms after the plan WHILE HOLDING the FOR UPDATE row lock; SMOKE_SIMULATE_STALE_READ — unlocked read, FOR UPDATE skipped, faithful pre-fix repro; both IS_PRODUCTION-inert, memory repo ignores them) + §18 four families, four pods :3216-:3219 on one PG (13 checks = boot + S 4 sequential await+assert-first + O 4 deterministic lock-handoff +100ms vs 400ms held lock + N 2 negative-control asserting the BUG signature + F 2 informational legal-set-only) + doc 12 v1.11.0 (TC-021/022 rewritten, TC-020 four pods, ENV rows, R2 127, inventory 180). §17 byte-untouched; false comment removed. LEAD REVIEW: worker's 4 judgment calls ALL ACCEPTED (F legal-set asserts = true invariants; N covers both races; TC-020 stale-claim true-up mandated by as-built policy; v1.10.0 ENV-row falsehood fixed live, changelog kept as history). **LEAD REVIEW CATCH**: F/race2's AUD-P01 assert demanded the RESET row unconditionally — false in the withdraw-won interleaving (trailing PUT lands draft-on-draft, audit-less; the withdrawal row carries AUD-P01). Exactly the verdict-3 defect class (flaky pass in worker's 127/127 run). Fixed in-commit to the interleaving-agnostic UNION (reset OR withdrawal row). Lead gates: tsc --strict 0 · build OK · smoke 108/108 (7.1s) · e2e 63/0/1 exit 0 (:3220 released) · PG-mode 127/127 ×2 (21.1s/21.2s, containers removed) · secrets+residue clean. NEXT: gate-brief §9 → codex re-gate 4 (NO merge until PASS) |
| 2 | W2-FIX-5 codex cycle-4 mandate: §18 test-barrier synchronization (O+N families) | worker-2 | COMPLETE 2026-09-11 — **c2a3f1a** (task #27, 2 files +266/−141; server.ts + §17 byte-untouched): barrier toolkit inside runW2Fix3Suite (direct `pg` client; pollBarrier bounded 5s/50ms, throws labeled on timeout — refuses to pass); `waitForIdleInTransaction` (first tx observed 'idle in transaction' = lock acquired + pause reached; the ONLY mid-tx-no-query session mid-check); `waitForBlockedQuery` (`wait_event_type='Lock'` + query LIKE '%FOR UPDATE%' — matched on query TEXT because the server sends parameterized SQL, the news id never appears literally; barrier client's own poll shows unsubstituted $1 and isn't Lock-waiting). Family O ×4 exact mandated sequence (fire first on :3218 → idle barrier → fire second on clean pod → blocked barrier → await FIRST + assert = explicit release → await SECOND + assert re-read outcome); Family N ×2 (stale op paused at idle barrier → PUT fired AND awaited+asserted 200 INSIDE the window → release → bug-signature asserts). `finalStateMeetsFixedInvariant` predicate extracted (draft + editedTitle + byte-same pods), reused by S/O/F asserts; N asserts the SAME invariant REJECTS both bug signatures (detection demonstration). F/race2 AUD-P01 tightened to exactly-one XOR (reset XOR withdrawal). `W2FIX4_SECOND_FIRE_OFFSET_MS` retired. False "Family S/O assertions would FAIL" claims removed everywhere (S cannot detect a race — sequential never overlap; against the sim only O overlap outcomes + the invariant would fail). Doc 12 v1.12.0 (barrier phrasing; 1.11.0 kept + trailing errata per annotate-not-rewrite; TC-NEWS-021/022 rewritten; ENV row barrier-aligned; inventory 180). **Worker's 6 flagged judgment calls ALL lead-ACCEPTED**: (1) blocked-query match on 'FOR UPDATE' text not id — parameterized SQL never shows the id, verified sound; (2) `(label, timeoutMs)` signature — forced by (1); (3) O/N PASS details quote polls/ms, S/F quote their coordination fact — honest reading, S/F have no barriers by design; (4) 1.11.0 errata-annotated not rewritten — v1.9.1 precedent; (5) O/N renamed W2-FIX-5, S/F/boot keep W2-FIX-4 — accurate provenance; (6) barrier-connect failure aborts suite exit 2 (loud) — §15 already proved PG up; silent skip would be fake completion. Lead verification greps: retired const 0 · false-claim strings 0 · "provably blocked" 0 · "zero timing assumptions"/"~100 ms" only in historical 1.11.0 entry + errata · §17 hunks ≥:1962 only. **Lead gates on c2a3f1a**: tsc --strict 0 · build OK · smoke 108/108 (7.1s) · e2e 63 PASS/1 FLAKY (S0-retry)/0 FAIL, :3220 released · PG-mode 127/127 ×2 (21.0s/21.5s, exit 0 both, containers removed) with SEPARATE per-run logs `.omc/reports/w2fix5-pg-lead-run-1.log`/`-2.log` (commit/date/duration/exit recorded; barrier observations quoted in every O/N PASS detail — idle 2 polls/~55ms, blocked 2 polls/~56ms, PUT 200 inside window, invariant-rejection reasons). Worker's own pre-commit evidence: tsc 0, build 0, 108/108, PG 127/127 ×2 (`w2fix5-pg-run-1/2.log`, on the then-uncommitted tree — superseded by lead runs on the commit). NEXT: gate-brief §10 → codex re-gate 5 (NO merge until PASS) — DONE: verdict-5 REVISE fix cycle 5 (decision #12); W2-FIX-6 dispatched (task #28) |
| 2 | W2-FIX-6 codex cycle-5 mandate: §18 test-controlled pause via PG advisory lock | worker-2 + lead fix | COMPLETE 2026-09-11 — **b96f926** (task #28, 3 files +464/−260: server.ts +28/−15, smoke-test.mjs +429/−239, doc 12 v1.13.0 +8/−6 incl. lead fix; scope = decision #12 items a-h, §17 byte-untouched — hunks at old :40 + ≥:1968). Server: `SMOKE_DELAY_NEWS_COMMIT_MS` RETIRED; `SMOKE_NEWS_PAUSE_ADVISORY_KEY` (positive int, `IS_PRODUCTION ? 0` + trunc/clamp, PG-topology only, memory repo ignores) — the committed transition executes `SELECT pg_advisory_xact_lock($1)` at the old sleep point; the HARNESS holds the session-level lock so the tx BLOCKS until explicit release; xact-level auto-release prevents pooled-connection leaks. Toolkit: `SET statement_timeout=2000` on the barrier client (finding 3 — every harness wait bounded); `acquirePauseGate` (pg_try_advisory_lock polled — absorbs the dying previous xact lock), `waitForPausedBackend` (paused tx identified by `wait_event_type='Lock'` + `query LIKE '%pg_advisory_xact_lock%'` + `pid <> pg_backend_pid()`, pid captured), `waitForBlockedBy` (`$1 = ANY(pg_blocking_pids(pid))` — identity upgrade), `releasePauseGate` (best-effort); `waitForIdleInTransaction`/`waitForBlockedQuery`/`W2FIX4_OVERLAP_DELAY_MS` retired (0 in script). O ×4 + N ×2 per-case try/finally: O = acquire → paused P1 → second fired → blocked BY P1 → **RELEASE (after blocked proof, BEFORE awaits)** → await+assert both; N = stale op HELD → PUT awaited+asserted 200 while HELD → release → clobber + invariant-REJECT; finally = release-then-bounded-drain. F/race2 counted exactly-one ROW (`resetCount + withdrawCount === 1`). **LEAD REVIEW CATCH (in-commit fix)**: the 1.12.0 historical changelog entry carried verdict-5-falsified claims ("its COMMIT is the explicit release", "edit now guaranteed to commit") without the annotate-not-rewrite errata the v1.9.1/v1.12.0 precedents require — bracketed v1.13.0 errata added. **All 10 worker judgment calls ACCEPTED** (release-then-drain order; pollBarrier first-row return; literal per-case try/finally; §18 intro update; TC-020 true-up; W2-FIX-6 naming on O/N only; kept W2FIX5_BARRIER_*/port const names; unquoted {polls,ms}; server retirement-note comment; changelog prefix move). **Lead gates on b96f926**: tsc --strict 0 · build OK (dist/server.cjs 158.5kb) · default smoke 108/108 (7.0s) · e2e 63 PASS/1 FLAKY (S0-retry)/0 FAIL exit 0 (:3220 released) · **PG-mode 127/127 ×2** with separate per-run logs `.omc/reports/w2fix6-pg-lead-run-1.log`/`-2.log` (19.0s/19.5s, exit 0 both, containers :55435/:55436 removed, tree-clean 0 recorded in each footer; every O/N PASS quotes paused pid + blocked-by-pid + polls/ms + gate-released-before-awaits + invariant-rejection reason "final state must be draft, got \"synced\""; F/race2 resetCount=1 withdrawalCount=0 both runs) · secrets/residue clean on the diff (doc `postgresql://…` placeholder only). Gate-brief §11 written (header/pin/§1/§6/working-tree note refreshed; §10.4 script-scoped errata; commit-scoped citations); codex re-gate 6 → **verdict-6 APPROVE** (decision #13); merge executed per §5 — see the W2-GATE-2 MERGE row |
| 2 | **W2-GATE-2 MERGE — feature/w2-1-dual-control → develop** | lead | **COMPLETE 2026-09-11 — merge commit `c99abfb`** (branch head `bb7b671`, 47 commits, `--no-ff`; authorization: codex verdict-6 APPROVE / decision #13). Conflicts (2, exactly as mapped by the dry-run): PROJECT-STATE.md → branch version + develop's decision #7 row re-inserted between #6 and #8 (decision log complete 1–13); src/components/AdminCMS.tsx :332 → resolved toward REMOVAL of the DCR-9-removed form state (`newsSyncToExternal`/`newsExternalCategory`; zero dangling references verified). **Standing checks on the RESOLVED tree (verdict-6 rider, pre-commit)**: tsc --noEmit 0 · build OK (dist/server.cjs 158.5kb) · default smoke 108/108 (7.0s, exit 0) · **PG-mode 127/127 ×2** (18.9s / 18.6s, exit 0 both, disposable postgres:16 containers :55437/:55438 removed; per-run logs `.omc/reports/w2merge-pg-lead-run-{1,2}.log`) · e2e 63 PASS / 1 FLAKY (known S0-retry) / 0 FAIL exit 0 (:3220 released) · secrets scan on the merge diff clean (3 `postgresql://` hits = the documented `user:pass` placeholder). develop now carries the full Wave-2 P0 chain: W2-1 dual control · W2-2 audit-endpoint removal · W2-3 audit coverage · DCR-9 Option C UI · W2-FIX-1..6. Feature branch retained (merged, no further work planned on it). Wave 2 remaining: W2-6 sync-webhook code phase (external blockers 1–8, brief §4) + Wave 3 deliverables 17–21 |
| 3 | Test/UAT/pentest/manual deliverables 17–21 | workers | STARTED 2026-09-11 (Wave-2 P0 merged; W2-6 external-blocked, does not gate) — plan `.omc/handoffs/wave3-plan.md`; sequencing L4→L5→L6 per doc 12 §11 |
| 3 | W3-1 L4 system-test execution (compose stack ENV-PROD-MODE, TC-SYS-001..006 + AVAIL-003/005) | worker-2 (fresh spawn) | COMPLETE 2026-09-11 — **7 PASS / 1 FAIL** (`.omc/reports/w3-l4/`: l4-summary.md + 8 per-TC logs + export.json + port-override.yml + teardown.log; provenance cac252e, image 1727c4fa8284, 09:44–09:53Z). PASS: SYS-001 (healthy ~5s, 10 tables), SYS-002 (content+session survive restart), SYS-003 (schema ×2 ON_ERROR_STOP=1 clean), SYS-004 (export→migrate counts match all 7 tables), SYS-006 (compose config + kustomize exit 0), AVAIL-003 (exact SIGTERM sequence, exit 0), AVAIL-005 (healthcheck gating + volumes). **FAIL: SYS-005 → DEF-001 (S2)**: abrupt PG termination crashes the app — node-pg BoundPool emits unhandled `'error'` (no `pool.on('error')` handler at server.ts:867) → process exits → Docker restart-loop (ENOTFOUND boot FATAL ×10); during outage BOTH /readyz and /healthz dead — breaks the 503-not_ready contract + NFR-AVAIL-001/002 (liveness must stay 200, no restart loops); recovery/data-durability/graceful-shutdown all fine. Port ruling: host 3000 held by the user's OTHER project (E-Leaning-LTC) — left untouched; stack ran 3221:3000 via gitignored `!override` compose override (recorded in provenance; container-internal port + healthchecks unaffected). Preflight observations: obs-1 (S4) bare-checkout kustomize needs the documented secret-copy step → rides to Doc 17 notes; obs-2 (S4) k8s/secret.yaml gitignored → **COMMITTED cac252e**. Boot-time fail-fast on unreachable PG is DESIGNED (AVAIL-004) — not a defect. L4 exit BLOCKED on DEF-001 → W3-FIX-1 |
| 3 | **W3-FIX-1 DEF-001 fix cycle: pg pool error handler (survive out-of-band PG termination)** | worker-2 code lane (fresh spawn) | IN FLIGHT 2026-09-11 — branch `feature/w3-fix-001-pool-error`. Scope: add `pool.on('error')` structured-warning handler at the PostgresRepository construction (server.ts:867 region) so an out-of-band server termination logs instead of crashing; process keeps serving (/readyz 503 via existing SELECT 1 probe, /healthz 200 liveness), pool reconnects on demand; boot-time fail-fast UNCHANGED (AVAIL-004); no swallowing of query-level errors (per-request 500 envelope unchanged). Regression = L4 TC-SYS-005 re-run on the fix (compose stop/start) + standing gates. NO commits (lead reviews + commits); docs untouched (doc 12 pins the contract correctly — code was wrong, no DCR) |
| 3 | W3-2 Doc 17 System Test Result | worker-5 | queued — needs W3-1 evidence |
| 3 | W3-3 L5 security lane + Doc 19 VA/Pentest draft | worker-5 | queued — L4 exit first (doc 12 §7) |
| 3 | W3-4 L6 UAT scenarios + Doc 18 | worker-1 | queued — L5 exit first |
| 3 | W3-5 Doc 21 User Manual (Thai-first) | worker-3 | queued — parallelizable after W3-2 |

## 4. CTO decision log (append-only)

| # | Date | Decision | Verdict |
|---|---|---|---|
| 1 | 2026-09-10 | Project charter: doc-first, 21-deliverable checklist, wave model; workers=flash-class executors; codex=CTO gate; lead sole merge authority | CHARTERED (lead, per user full-authority grant) |
| 2 | 2026-09-10 | DCR-1..5 triage (see §8). DCR-3 maker-checker bypass CONFIRMED in code by lead (server.ts:1306) → as-built documented everywhere, dual-control enforcement queued as Wave-2 P0 backlog, CTO ratifies at Wave-1 gate | TRIAGED (lead) — pending CTO ratification |
| 3 | 2026-09-10 | Lead review of 12-doc set PASSED: REQ SRS↔RTM 1:1 (94); all RTM TC refs resolve in doc 12 (168 TC defs, 21 series); UAT-001..065 mapped; DCR numbering unified to §8 register (strict-mode=DCR-6); RTM FR-NEWS-009 TC ref fixed to TC-SEC-011; role matrices 08↔09 agree; 9 tables + 13 components covered; HANDOVER fixed per DCR-1/2 | REVIEW PASSED (lead) → to CTO gate |
| 4 | 2026-09-10 | codex CTO gate ABORTED — account usage limit hit (resets 2026-09-15 09:01 or on credit top-up); no verdict produced (.omc/artifacts/cto-gate-wave1.md). SUBSTITUTE: Wave-1 docs gate runs via independent opus critic agent (read-only, fresh context, same verdict contract — not lead self-approval). codex remains BINDING for auth/security/data-lane CODE merges in Wave 2; if still rate-limited at that point, merges hold until codex returns or user tops up credits (surfaced to user) | OUTAGE PROTOCOL (lead, per full-authority grant) |
| 5 | 2026-09-10 | Substitute CTO gate verdict = **REVISE** (full verdict: .omc/artifacts/cto-gate-wave1-substitute.md). All 8 DCRs RATIFIED. STRICT dual-control ruling adopted (no role incl. admin reaches 'synced' outside checker approve; state+identity guards; workflow fields server-controlled; break-glass = separate future requirement). Wave-2 priorities ratified: P0 dual-control bundle (DCR-3+7 + AUD-P01/02/03) → DCR-8 (prefer REMOVAL of POST /api/audit-logs) → AUD-P06/05/07; P1 strict mode + shared rate-limit store + sync webhook; P2 envelope + RWX + migration tooling; P3 dead-enum + audit hardening + DB immutability. 3 blockers (SRS FR-NEWS-009 carve-out; doc 12 TC-SEC-011 carve-out; doc 07 §12 DCR numbering) → fix round dispatched to workers 2/5/4 + RISK-022/023 to worker-1; critic re-review limited to the three fixes → APPROVE flip | REVISE — fix round 1 of 3 (lead executed, per decision #4 authority) |
| 6 | 2026-09-10 | Substitute CTO gate re-verification of fix round (commit 43d8591): **APPROVE** — all 3 blockers closed exactly as ruled, no new defects; regression integrity held (94 REQ, 168 TCs, as-built pins intact). Merge to develop AUTHORIZED; Wave-2 decomposition authorized per ratified priorities. **Conditions riding the approval:** (1) Wave-2 auth/security/data merges still require codex PASS — DCR-3+7 and DCR-8 sit in that lane (hold if codex limited, per #4); (2) flip-test discipline — when TC-NEWS-011/CMS-008/SEC-012/013 pins flip due to fixes, linked DCR/REQ/TC statuses in docs 03/04/12 flip in the SAME PR; (3) resolve rejected-item re-submission UX during P0 decomposition (doc 09 §8.3 rule 4), not after implementation; (4) P0 order as ratified. Watch items: strip workflow fields at payload-validation layer (governs future bulk endpoints too); tighten ~74 non-RTM TC→REQ mappings to specific IDs before Doc 17 | APPROVE (substitute critic; codex binding for W2 code merges unchanged) |
| 7 | 2026-09-10 | Lead rulings under delegated authority (all ride to codex at the W2 code gate): (1) **non-draft edit ⇒ draft** — any PUT on pending_approval/synced/rejected news resets to draft, all stamps cleared, syncToExternal:false (closes stale-approval-live-content + in-review TOCTOU edit; implemented in 22023eb); (2) **AUD-P07 trim** — audit 401s only when a kbj_session cookie was presented and failed validation; all authenticated 403s unconditional; no-cookie 401s request-log-only (5k-cap eviction risk under probe storms; actorless events); (3) **audit-union prune** — CREATE/DELETE/SYNC_PUBLIC removed in the W2-3 single types.ts edit (dead post-DCR-8; CREATE/DELETE are sync-log namespace, not audit writers; UPDATE stays live via AUD-P01); P1/P2 content-CRUD vocabulary = CONTENT_* values, bare UPDATE migrates to CONTENT_UPDATE atomically at that phase; (4) W2-2 code phase reassigned to worker-2 for lane parallelism (worker-5 remains spec owner); (5) while the codex hold lasts, W2-2/W2-3 doc+code stack on feature/w2-1-dual-control — one branch = the full P0 state codex reviews | LEAD RULINGS — pending codex ratification at gate |
| 8 | 2026-09-11 | **codex CTO gate Wave-2** (head 66380a7, 22 commits; brief `.omc/artifacts/cto-gate-wave2.md`; verdict `.omc/artifacts/cto-gate-wave2-verdict.md`): **REVISE** — ALL 10 ratification asks RATIFIED (decision #7 five sub-rulings; W2-5 fail-open; requireRole 403-audit try/catch; §16/§14 ordering; DCR-9 Option C policy incl. withdraw-by-maker-without-checker — "withdrawal must preserve current content and remain audited"; DCR-10 DEFER). 4 blockers, each reproduced in isolation by codex: (1) non-atomic workflow transitions — approve races a concurrent PUT and overwrites the newer draft with stale content as synced (server.ts:909/938/1673/1714); (2) legacy pending items without submittedBy bypass both self-decision guards — original admin submitter can approve own pre-migration submission (server.ts:470/1696/1769); (3) withdrawal sends the full stale browser snapshot via PUT — silently replaces concurrent edits; already-draft withdraw yields no AUD-P01 (App.tsx:291, server.ts:1565); (4) state commits before recordAudit — audit-insert failure leaves an unaudited draft; retry skips the audit; approval same (server.ts:1582/1588/1714). Fix direction (codex-instructed): transactional locking or version-checked transitions across edit/submit/approve/reject rejecting stale ops; deny decisions lacking verified submitter identity (fresh submission cycle); state-only withdrawal from current server content with atomic synced precondition; commit state+audit atomically (same transaction PG / same critical section in-memory); regression coverage for concurrency, legacy-submission, stale-withdrawal, audit-failure; then re-run gates + return. Advisory: W2-6 design must address external withdrawal propagation (local draft reset must remove an already-delivered public copy). **NO MERGE authorized**; §5 conflict plan stands for subsequent APPROVE | REVISE — fix cycle 1 |
| 9 | 2026-09-11 | **codex W2-GATE-2 re-gate** (head `dda8268`, 32 commits; same brief refreshed; verdict `.omc/artifacts/cto-gate-wave2-verdict-2.md`, raw `.raw.md`): **REVISE — fix cycle 2**. Blockers **2+4 CLOSED** (codex-verified: legacy 409+ACCESS_DENIED both endpoints; PG one-tx state+audit+synclog w/ ROLLBACK, memory restore, `next(err)`). Blockers **1+3 NOT CLOSED (cross-pod)**: mutex is process-local (server.ts:1400); PG tx begins AFTER reads/guards; UPDATE predicate only `WHERE id=$1` (:789/:1011) — codex isolated two-pod repro: stale approve commits `Old content/synced` over a concurrent edit's `New edit/draft`; stale withdraw 200-after-precondition-lapse commits `Old content/draft` (:1821/1878/1907; :2010/2022/2042). **§7.6 ruled BLOCK**: `k8s/deployment.yaml:12` `replicas: 2` = existing invariant violation in the shipped topology — cannot ride as documented limitation. **Fix direction (codex-mandated)**: `SELECT … FOR UPDATE` re-read INSIDE the `commitNewsTransition` transaction + state/identity validation + next-state construction + state/audit writes in the SAME tx across ALL writers incl. ordinary draft PUTs ("merely locking after constructing the replacement is insufficient"); optimistic version column acceptable alternative; **deterministic tests vs TWO server processes sharing PostgreSQL covering both reproduced races** (current §17 PG race hits one server, smoke-test.mjs:1911 — cited gap). ALL §3 riders RATIFIED (#7.1–7.5, D5 as design amendment, D6, D7 w/ correction — design's "only the create default sets externalCategory" is OVERSTATED, PUT still accepts/persists it, exclusion unaffected, D8/T5 incl. FORCE_SYNC desired-set reconciliation, W2-5 fail-open CONFIRMED). **Merge NOT authorized; §5 plan stands for eventual APPROVE.** W2-FIX-3 dispatched to a fresh worker-2 spawn (prior agent pool lost to compaction) | REVISE — fix cycle 2 |
| 10 | 2026-09-11 | **codex W2-GATE-2 re-gate 3** (head `d40b197`; brief §8 = cycle-2 response; verdict `.omc/artifacts/cto-gate-wave2-verdict-3.md`): **REVISE — fix cycle 3. ONE blocker**: the two first-op-not-awaited staggered §18 cases (smoke-test.mjs:2117 race-1 approve-first, :2270 race-2 withdraw-first) are scheduler-dependent — codex reproduced the flake with mocked delayed first requests (both yielded legit approve-400/withdraw-409 while assertions demand 200); brief §8.4's "awaited to completion" claim was WRONG for these two cases. **Code merits CONFIRMED**: §8.1 executor verified structurally (with claim-narrowing: stale CLIENT payloads on ordinary PUTs remain last-writer-wins — version precondition = future work, NOT a blocker per codex); §8.2 blocker-1 race CLOSED in code; §8.3 blocker-3 race CLOSED in code + **replicas:2 architectural BLOCK RESOLVED** (serialization now in PostgreSQL); blockers 2+4 remain closed; zero route-path saveNews callers (fixture-seed only, :2997). Brief errata ordered: §18 fingerprint claim narrowed (race 1 compares the two pod GETs); "§18 sets neither hook" FALSE (both pods set SMOKE_SEED_W2FIX1_FIXTURES :1998/:2003; audit-injection unset); standing-gate evidence citation fixed; observed race-1 concurrent per artifact = [400,400,400]. **Fix mandate (codex)**: await+assert first response before the second request (sequential branch coverage); controlled synchronization for the overlap/lock-handoff (NOT 3× Promise.all); demonstrate the regression DETECTS the former stale-read implementation; correct TC-NEWS-021/022 (doc 12 :294/:295); rerun PG coverage. §5 merge-plan updates: AdminCMS resolve toward REMOVAL of the DCR-9-removed form state (:332 — §5 account stale); standing checks on the RESOLVED merge tree incl. PG §18. **Merge NOT authorized.** W2-FIX-4 dispatched (task #26, worker-2) | REVISE — fix cycle 3 |
| 11 | 2026-09-11 | **codex W2-GATE-2 re-gate 4** (head `aad9c88`, **40 commits** — codex corrected my 39 pin: cycle-3 delta = `2ce95ae`+`fb3d748`+`aad9c88`; brief §9 = cycle-3 response; verdict `.omc/artifacts/cto-gate-wave2-verdict-4.md`): **REVISE — fix cycle 4. TWO blockers, same class**: (1) Family O overlap still assumes the first request acquires the FOR UPDATE lock within the fixed 100 ms sleep — codex's mocked delayed-first repro yields a legit approve-400 vs asserted 200, and a second-request-after-commit passes with ZERO overlap exercised; (2) Family N uses the same sleep coordination — the competing edit is not guaranteed to commit inside the sim window, and the N check's claim that Family **S** would fail against the sim is FALSE (sequential requests never overlap → cannot detect a race; only the overlap assertions + the final-state invariant can). **CONFIRMED clean**: all four S cases, hook guarding (PG-only, production-dead), F/race2 union interleaving-agnostic (notes existence-not-exactly-one cardinality), transactional behavior intact, original production-code blockers remain closed, §17 untouched, node --check + strict tsc pass. **Fix mandate (codex)**: replace elapsed-time coordination with a TEST BARRIER — confirm the first tx acquired the row lock AND reached its pause, issue the second request, confirm its PG query is BLOCKED by that tx, explicitly release the first; apply to all four O cases with bounded timeouts + cleanup; for N: pause the sim op after its unlocked read, AWAIT+ASSERT the competing edit's committed response, then release the stale op, and demonstrate the SAME final-state invariant used by the fixed regression rejects BOTH resulting bug signatures; align doc-12/brief "zero timing assumptions"/"provably blocked" phrasing to the barrier design and REMOVE the sequential-detects-the-race claim; rerun PG coverage retaining SEPARATE per-run evidence (run, commit, duration, exit status, observed synchronization). **Merge NOT authorized.** W2-FIX-5 dispatched (task #27, worker-2) | REVISE — fix cycle 4 |
| 12 | 2026-09-11 | **codex W2-GATE-2 re-gate 5** (head `58c4bb2`, 43 commits; brief §10 = cycle-4 response; verdict `.omc/artifacts/cto-gate-wave2-verdict-5.md`, raw `-5.raw.md`): **REVISE — fix cycle 5. TWO blockers**: (1) Family O still lacks EXPLICIT release control — the 400 ms hook timer (server.ts:1077) resumes automatically; the harness awaits (:2326/:2346/:2370/:2391) consume already-running promises and control nothing; every case still depends on fire+poll fitting inside the timer window. Mandate: a test-controlled pause HELD until the harness explicitly releases it after observing the blocked second request. (2) Family N same class — the stale op's timer runs independently of the PUT await (:2423/:2453); if PUT exceeds the remaining window the stale op commits first. Mandate: HOLD the sim after read/plan until PUT=200 is asserted, then explicitly release. **Detection demonstration CONFIRMED FIXED** (shared predicate :2203 reused + rejects both signatures). **MAJORS**: (3) barrier identity/timeout/cleanup — idle-in-transaction is not unique to the intended pause (can occur between statements); the blocked query is not tied to the target backend or its blocker (verify the blocking relationship); :2059 awaits an unbounded DB query before the deadline check; failed checks continue without per-case draining (cleanup only at :2536) — identify paused tx + blocked backend explicitly, bound DB waits, release/drain per case in finally. (4) live docs/brief overstate ("explicit release", guaranteed in-window — doc 12 :5/:131/:297/:298, brief :406); also my §10.4 unqualified "retired-const 0-match" claim inaccurate (doc 12's retirement NOTE mentions it — script-only claim needed). **MINORS**: (5) F/race2 XOR checks exclusive CATEGORIES not exactly-one ROW (.find() — two reset rows pass; count and assert total = 1); (6) brief :392 range diff `aad9c88..c2a3f1a` shows 3 files (includes a9efd14 state commit) — cite the COMMIT `git show --stat c2a3f1a` (2 files) instead. **NIT**: (7) per-run evidence retention correct; rerun after fixes. **CONFIRMED**: pin 58c4bb2/43/clean; §17 byte-identical; strict tsc; no files modified. **Fix design (lead)**: PG advisory-lock pause — NEW `SMOKE_NEWS_PAUSE_ADVISORY_KEY` hook (positive int, non-prod, PG-only: the transition executes `pg_advisory_xact_lock($key)` at the old delay point; the HARNESS holds the session-level lock, so the tx blocks — row lock held in normal mode, lock-free window held in sim mode — until the harness unlocks = EXPLICIT release; xact-level auto-releases at COMMIT so pooled connections can't leak it); RETIRE `SMOKE_DELAY_NEWS_COMMIT_MS` + `W2FIX4_OVERLAP_DELAY_MS`; barrier upgrades — paused backend identified by `query LIKE '%pg_advisory_xact_lock%' AND wait_event_type='Lock'` (returns its pid), blocked second tied via `$1 = ANY(pg_blocking_pids(pid))` against that pid, `SET statement_timeout=2000` on the barrier client (bounded waits), per-case try/finally drain+release; F/race2 count-based exactly-one; doc 12 v1.13.0 + brief §11 aligned (commit-scoped diff citation). **Merge NOT authorized.** W2-FIX-6 dispatched (task #28, worker-2) | REVISE — fix cycle 5 |
| 13 | 2026-09-11 | **codex W2-GATE-2 re-gate 6** (head `9cfc967`, 46 commits; brief §11 = cycle-5 response; verdict `.omc/artifacts/cto-gate-wave2-verdict-6.md`, raw regate6 log): **APPROVE — CTO PASS.** All 7 verdict-5 findings CLOSED: (1) blocker 1 — advisory-lock pause at server.ts:1081, hook production-disabled :1290, all four O cases unlock after the blocked observation and before the awaits (:2401/:2443/:2486/:2526), no timer window remains; (2) blocker 2 — N awaits+asserts PUT=200 while held (:2579/:2627), release at :2581/:2629, both negative controls reproduce + reject; (3) major 3 — statement_timeout :2074, waiting-advisory-query identity :2130, pg_blocking_pids tie :2150, release-then-bounded-drain in all six cases; (4) major 4 — doc 12 :132/:297/:298 as-built, errata :6, retired-var counts confirmed 0 script / 1 server comment / 3 doc mentions; (5) minor 5 — counted `resetCount + withdrawCount === 1` :2716; (6) minor 6 + standing integrity — commit-scoped citations, b96f926 exactly 3 files, server.ts confined to the two hook regions, §17 + shared predicate byte-identical, codex-reran strict tsc + syntax checks, prior closures intact; (7) nit 7 — both lead PG logs substantiate 127/127 (19.0s/19.5s, exit 0) with the demanded per-run synchronization details. **One non-blocking NIT**: brief metadata — doc 12 numstat is +7/−6 not +8/−6 (the lead errata landed inside an already-modified line), and §11.9's "touch neither" missed that 9cfc967 changes PROJECT-STATE.md; both corrected in the brief. Codex re-verified pin 9cfc967/46/clean, no files modified. **"CTO PASS authorizes the lead's merge under §5/§11.9, retaining all prior ratifications and riders, including standing checks on the resolved merge tree with PG 127/127."** Merge executed by the lead the same day (W2-GATE-2 board row) | APPROVE — merge authorized |

## 8. DCR register (doc change requests)

| DCR | Source | Finding (verified against code) | Disposition |
|---|---|---|---|
| DCR-1 | worker-4 | Export field is `exportTimestamp` (server.ts:2072), not `generatedAt` (HANDOVER §5 wrong) | ACCEPT — fix HANDOVER §5 in W1 review pass |
| DCR-2 | worker-4 | No `PUT /api/documents` route exists; HANDOVER §5 implies it | ACCEPT — fix HANDOVER §5 |
| DCR-3 | worker-4, confirmed by lead | `POST /api/news` (and update) with `syncToExternal=true` creates directly as `externalSyncStatus:'synced'` (server.ts:1306,1315,1346) — maker bypasses checker dual control (BOT governance violation). **Corollary (worker-5, verified):** bypass path writes ONLY sync_logs (admin-only visibility) and NO audit entry — invisible to checkers | ACCEPT as finding — **CTO-RATIFIED w/ conditions**: STRICT semantics (no role incl. admin reaches 'synced' outside approve; approver≠submitter guard; submit draft-only; workflow fields stripped/server-controlled); AUD-P01/02/03 audit coverage in the SAME change; Wave-2 P0 bundle with DCR-7; RISK-021 |
| DCR-4 | worker-4 | Response envelope inconsistent — reads & some 404s return bare data, no `success` field | ACCEPT — doc 08 as-built; [PLANNED] normalization Wave-2 P2 |
| DCR-5 | worker-4, corrected by CTO gate | TS union declares `'pending'` as the ONLY dead member (there is NO 'approved' member — src/types.ts:28); runtime values: draft/pending_approval/synced/rejected | ACCEPT — RATIFIED with correction; docs aligned; dead-union cleanup Wave-2 P3 |
| DCR-6 | worker-2, confirmed by lead | `tsconfig.json` has NO `"strict": true` (nor strictNullChecks) — "TypeScript strict" is convention only; code passes tsc in non-strict mode | ACCEPT — enable strict + fix fallout in Wave-2 P1 (MAINT quality gate); SRS states as-built + [PLANNED] |
| DCR-7 | worker-5, confirmed by lead | NO server-side state guards on maker-checker: approve/reject callable from ANY state (server.ts:1407/1444); submit-approval accepts any state (1384); PUT spread overwrites approvedBy/approvedAt (1338-1342) | ACCEPT — **CTO-RATIFIED**; Wave-2 P0 bundle WITH DCR-3 (one work item); scope adendum: submit guarded to draft, workflow fields stripped on create/update, approver≠submitter guard; RISK-022 |
| DCR-8 | worker-5, confirmed by lead | `POST /api/audit-logs` (server.ts:1670, admin) appends arbitrary audit rows under an "Immutable" comment (1665) — audit-trail integrity risk | ACCEPT — **CTO-RATIFIED w/ instruction**: PREFER REMOVAL of the endpoint over restriction; if break-glass retained, the append act itself must be audit-logged and role-scoped below blanket admin; Wave-2 P0; codex PASS mandatory; RISK-023 |
| DCR-9 | worker-3 (.omc/research/inert-sync-toggles-ux.md), confirmed by lead | Post-W2-1 the external-sync UI is worse than inert: form toggle inert but chip claims "Auto-sync enabled"; DRAFT-row toggle FABRICATES synced state client-side (App.tsx:307 discards server response; fake "now LIVE" toast; fabricated SyncLog); SYNCED-row toggle = accidental unpublish via forced reset; create/update toasts keyed on payload flag not server response (App.tsx:227-275) — UI lies about publication state in a BOT-governed workflow | ACCEPT — **lead-ruled (2026-09-10, full-authority grant)**: Option C — remove publish-promising toggles; add honest "Withdraw from public" row action riding existing PUT forced-reset semantics (maker/admin RBAC inherited, AUD-P01 fires); fix response-discard + fabricated-log bugs. Doc-first revision BEFORE code (doc 11 + ripple 03/04/12 + docs 05/06 truth). **Doc phase DONE 36ad1eb**; code phase IN FLIGHT (task #19, worker-4). Withdraw-by-maker-without-checker + the whole ruling ride to codex with the P0 chain |
| DCR-10 | worker-3 (repo-wide grep evidence, task #17), confirmed by lead | `NewsItem.externalCategory` has ZERO consumers: union def (types.ts:29), stored/round-tripped (server row-mapper/save/default), written by the AdminCMS form, seeded in initialData — nothing reads it for display, filter, or export (public view filters `item.category`); its only UI entry point was the DCR-9-removed toggle's form select (doc 11 §5.9 documents the fact) | ACCEPT as finding — **lead-ruled (2026-09-10): DEFER** — removal decision blocked until the W2-6 webhook payload is specified (the outbound public-site sync is the only plausible consumer); if the W2-6 payload does not use it, removal rides the P1/P2 CONTENT_* audit-vocabulary migration (single types/schema/server pass). No action now; DCR-9 code removes only the form select. Rides to codex with the chain. **DEFER CONDITION RESOLVED 2026-09-11**: W2-6 design v0.3 (worker-4) recommends exclusion — lead ACCEPTED. `externalCategory` stays OUT of the webhook payload (it already carries `category`, which the public view filters on); removal rides the P1/P2 CONTENT_* migration. Evidence: fresh post-DCR-9 grep — only plumbing remains (types.ts:29, server.ts:454/636/917/925/936/943/1532, schema.sql:100, migrate.js:27/49/202, seeds); zero UI/e2e consumers; nothing sets it except the frozen 'press-release' default. Caveat: revisit only if public-web ratifies a taxonomy enum not derivable from `category`. Rides to codex W2-GATE-2 |

## 5. Git protocol

- `main` = pristine upstream; `develop` = integration; `feature/*` per wave/task.
- **Lead reviews + merges everything. Workers never commit/merge.**
- Merge gates: `tsc --noEmit` green, smoke tests green, build green, secrets
  scan clean; auth/security/data lanes additionally require codex PASS.
- Fix cycles until PASS — never merge on BLOCKING.

## 6. Resume procedure (new session)

1. Read this file top-to-bottom.
2. `state_read(mode="team")` for OMC team phase; read `.omc/handoffs/*.md`.
3. `git branch --show-current` + `git status --short` — verify on expected branch.
4. `TaskList` — reconcile board vs §3 wave board; resume the last non-terminal
   stage. Never respawn workers that already reported completion.

## 7. Doc conventions (workers follow exactly)

- Location `docs/deliverables/NN-slug.md`; English body (repo convention),
  Thai-first for user-facing strings quoted in docs.
- Header block: Version / Status (Draft→Reviewed→Approved) / Date / Author.
- Docs describe the system AS BUILT (source of truth: `server.ts`, `src/`,
  `scripts/schema.sql`, `README.md`, `HANDOVER.md`); future work marked `[PLANNED]`.
- If code reveals a doc problem → file a DCR to the lead (doc §section,
  problem, proposed fix) → CTO decides → doc revised FIRST (version bump),
  then coding resumes. No silent divergence.
